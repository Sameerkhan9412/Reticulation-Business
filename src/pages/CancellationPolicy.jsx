import React from 'react';
import { Navigate } from 'react-router-dom';

const CancellationPolicy = () => {
  return <Navigate to="/refund-cancellation-policy" replace />;
};

export default CancellationPolicy;